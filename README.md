# Audio

See branches to check different task.

## `automatic_speech_recognition` branch layout

- `asr_toronto/` — Toronto Ukrainian ASR (Whisper-base + Lightning)
- `ast_level1_fleurs/` — AST Level 1: EN -> UK on FLEURS (`lab3_additional.ipynb`)
- `ast_level2_librispeech/` — AST Level 2: new LibriSpeech-UA dataset + fine-tune + COMET eval
- `Lab_3_report.pdf` / `report.tex` — combined report covering all three tasks
